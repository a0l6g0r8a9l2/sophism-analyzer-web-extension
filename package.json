{
  "name": "sophism-analyzer",
  "version": "1.0.0",
  "description": "Chrome extension for analyzing fallacies in YouTube videos using AI",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
